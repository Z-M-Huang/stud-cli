# Banned-vocab clean fixture

This file uses only the allowed synonyms: bundled, core, first-party,
reference, default, immutable, attached, loaded, active. The scanner must
exit 0 against this directory.
